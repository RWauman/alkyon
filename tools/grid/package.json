{
  "name": "alkyon-grid-bundle",
  "private": true,
  "version": "0.0.0",
  "description": "Builds src/ui/vendor/glide-data-grid.js. Not needed to build or run alkyon — the output is committed, exactly like every other vendored library.",
  "scripts": {
    "build": "node build.mjs"
  },
  "dependencies": {
    "@glideapps/glide-data-grid": "6.0.3",
    "lodash": "4.17.21",
    "marked": "4.3.0",
    "react": "18.3.1",
    "react-dom": "18.3.1",
    "react-responsive-carousel": "3.2.23"
  },
  "devDependencies": {
    "esbuild": "0.25.10"
  }
}
